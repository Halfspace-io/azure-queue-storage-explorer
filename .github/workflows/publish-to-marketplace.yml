name: Publish to VS Code Marketplace

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag to publish (e.g., v1.0.0)'
        required: true
        type: string

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Install pnpm
      uses: pnpm/action-setup@v2
      with:
        version: latest
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'pnpm'
        
    - name: Install dependencies
      run: pnpm install
      
    - name: Compile TypeScript
      run: pnpm run compile
      
    - name: Get release tag
      id: release-tag
      run: |
        if [ "${{ github.event_name }}" = "release" ]; then
          RELEASE_TAG="${{ github.event.release.tag_name }}"
        else
          RELEASE_TAG="${{ github.event.inputs.release_tag }}"
        fi
        echo "release-tag=$RELEASE_TAG" >> $GITHUB_OUTPUT
        echo "Publishing release: $RELEASE_TAG"
        
    - name: Check if already published
      id: check-published
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        RELEASE_TAG="${{ steps.release-tag.outputs.release-tag }}"
        
        # Check if this version is already published
        if gh release view "$RELEASE_TAG" --json assets --jq '.assets[] | select(.name | contains(".vsix")) | .name' | grep -q ".vsix"; then
          echo "already-published=true" >> $GITHUB_OUTPUT
          echo "✅ Release $RELEASE_TAG already has .vsix assets"
        else
          echo "already-published=false" >> $GITHUB_OUTPUT
          echo "📦 Release $RELEASE_TAG needs to be packaged"
        fi
        
    - name: Package extension
      if: steps.check-published.outputs.already-published == 'false'
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        # Install vsce
        npm install -g @vscode/vsce
        
        # Package extension
        vsce package --no-dependencies
        
        # Upload to release
        for file in *.vsix; do
          if [ -f "$file" ]; then
            gh release upload "${{ steps.release-tag.outputs.release-tag }}" "$file"
            echo "📦 Uploaded $file to release"
          fi
        done
        
    - name: Publish to VS Code Marketplace
      env:
        VSCE_PAT: ${{ secrets.VSCE_PAT }}
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        # Install vsce
        npm install -g @vscode/vsce
        
        # Get release tag
        RELEASE_TAG="${{ steps.release-tag.outputs.release-tag }}"
        
        # Check if release exists
        echo "🔍 Checking if release $RELEASE_TAG exists..."
        if ! gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
          echo "❌ Release $RELEASE_TAG not found. Please ensure the auto-release workflow has completed successfully."
          exit 1
        fi
        
        # Find the .vsix file in release assets
        echo "🔍 Looking for .vsix file in release assets..."
        VSIX_FILE=$(gh release view "$RELEASE_TAG" --json assets --jq '.assets[] | select(.name | contains(".vsix")) | .name' | head -n1)
        
        if [ -z "$VSIX_FILE" ]; then
          echo "❌ No .vsix file found in release $RELEASE_TAG"
          echo "Available assets:"
          gh release view "$RELEASE_TAG" --json assets --jq '.assets[].name'
          exit 1
        fi
        
        echo "✅ Found .vsix file: $VSIX_FILE"
        
        # Download the .vsix file
        echo "📥 Downloading $VSIX_FILE from release..."
        gh release download "$RELEASE_TAG" --pattern "$VSIX_FILE"
        
        # Publish to marketplace
        echo "🚀 Publishing $VSIX_FILE to VS Code Marketplace..."
        vsce publish --packagePath "$VSIX_FILE"
        
        echo "✅ Successfully published $VSIX_FILE to VS Code Marketplace!"
        
    - name: Update release notes
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        RELEASE_TAG="${{ steps.release-tag.outputs.release-tag }}"
        
        # Add marketplace link to release notes
        MARKETPLACE_URL="https://marketplace.visualstudio.com/items?itemName=wojciechciok.azure-queue-storage-explorer"
        
        # Get current release body
        CURRENT_BODY=$(gh release view "$RELEASE_TAG" --json body --jq '.body')
        
        # Add marketplace link if not already present
        if ! echo "$CURRENT_BODY" | grep -q "marketplace.visualstudio.com"; then
          NEW_BODY="$CURRENT_BODY"$'\n\n'"## 📦 VS Code Marketplace"$'\n'"[Install from VS Code Marketplace]($MARKETPLACE_URL)"
          
          gh release edit "$RELEASE_TAG" --notes "$NEW_BODY"
          echo "✅ Updated release notes with marketplace link"
        fi
